# 自分の得意な言語で
# Let's チャレンジ！！
input_line = gets.chomp
n, m = input_line.split.map(&:to_i)

distances = []
(n - 1).times do
  distance = gets.chomp.to_i
  distances << distance
end

# 変数に代入
a = n
b = m
z = 0


d=a-1
e=d

d.times do
  c=distances[e-1]
  if c<=b then
   z=z+c   
  end
  e=e-1
end
puts z