input_line = gets.chomp
numbers = input_line.split.map(&:to_i)
A = numbers[0]
B = numbers[1]
C = numbers[2]

i=1

while i <= A
    if i%B==0&&i%C==0
        puts "AB"
    elsif i%B==0
         puts "A"
    elsif i%C==0
        puts "B"    
    else
        puts "N"
    end
i += 1
end     